import { useState } from 'react';
import { useFetch } from '../hooks/useFetch'

import './Header.css';

function Header() {
   
    return (
        <header className="Header">
            <h1>Andreas J.'s SkyBox Submission</h1>
        </header>
    );
}

export default Header;
